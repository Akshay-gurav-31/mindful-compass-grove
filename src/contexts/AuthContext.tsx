
// This file now serves as an entry point to the auth context
// Exporting the Auth Provider and useAuth hook
import { AuthProvider, useAuth } from './AuthProvider';
export { AuthProvider, useAuth };
